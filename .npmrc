# Memungkinkan paket dependencies hoisted ke root node_modules
# Ini membantu kompatibilitas dengan package yang memerlukan peer dependencies
shamefully-hoist=true

# Tidak memerlukan peer dependencies yang ketat
# Membantu menghindari konflik versi
strict-peer-dependencies=false

# Otomatis menginstall peer dependencies
# Menghindari warning missing peer dependencies
auto-install-peers=true

# Menggunakan node_modules biasa daripada symlink
# Meningkatkan kompatibilitas dengan tools lain
node-linker=hoisted
